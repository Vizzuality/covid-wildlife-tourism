<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<meta name="description" content="">
		<title><%= [yield(:page_title).presence, t('views.layout.application.title')].compact.join(' | ') %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <header>
      <%= render 'shared/nav' %>
    </header>
    <main role="main" class="nav-spacer">
      <% if Rails.env.development? %>
        <%= switch_user_select %>
      <% end %>
      <% if flash[:notice] %>
        <div class="alert alert-info"><%= flash[:notice] %></div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
